import { Typography } from "ui";

export default function Home() {
  return (
    <div>
      <Typography variant="h2">Home</Typography>
      <Typography variant="body1">
        This is the home page inside the shell app.
      </Typography>
    </div>
  );
}
